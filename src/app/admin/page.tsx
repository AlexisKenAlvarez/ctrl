import AdminDashboard from "@/components/AdminDashboard";

const page = async () => {
  return (
    <div className="flex flex-1 w-full">
      <AdminDashboard  />
    </div>
  );
};

export default page;
